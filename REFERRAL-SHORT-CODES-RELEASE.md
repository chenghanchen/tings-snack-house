# 推荐奖励页与 8 位短码

> 历史发布记录：后续方案已改为唯一正式码 `TSHREF-` 加 6 位字符，旧码失效，不再保留别名。新升级请遵循 `REFERRAL-SIX-DIGIT-RELEASE.md`，不要重跑本旧方案。

本地实现不代表生产已升级。发布时先执行数据库升级，再发布前端。

## 本次生产核验（2026-09-15）

已在现有 Supabase 项目执行短码升级：1 个既有已验证账户已补齐短码，缺失数量为 0。升级前后旧推荐记录（排除新增 short_code 字段）、订单、奖励事件和优惠券的数据指纹一致；新旧码只读预览结果一致且有效。未创建真实订单，生成器仍不向匿名或普通登录用户开放。前端发布状态以本次网站部署核验为准。

## 数据库上线

1. 确认已部署 `customer-wallet-migration.sql` 和 `coupon-claims-v2-migration.sql`，保留生产备份。
2. 执行 `account-referral-short-codes-migration.sql`。脚本使用单个事务，可重复执行；订单函数不匹配时停止，不跳过检查。
3. 为现有已验证账户补充唯一、固定的 8 位大写字母/数字短码，排除易混淆的 I、O、0、1。新验证账户也自动获得短码。
4. 原 `referral_code` 不修改，新增 `short_code` 别名。新旧码均走原有资格检查、订单计价和奖励记录，不能通过换码重复领取。
5. 钱包和新订单返回短码；历史订单快照保持原样。推荐短码不可转让或重新生成，并防止与优惠券代码冲突。
6. 核验登录账户获得 8 位码；只读预览检查新旧码结果相同；不得为验收创建真实顾客订单。

## 页面

- 仅推荐奖励面板上下内边距改为 20px，电脑和手机采用一致规则；其他账户面板和横向留白不变。
- 更新两段说明，删除日常刷新按钮；每次进入面板自动加载，网络失败仍可重试。
- 发布前运行 `npm run ci:check -- <完整 base SHA>`，已包含账户浏览器回归。
- 若只回退前端，保留短码数据库升级，避免已分享的新短码失效。不要重跑旧钱包/营销迁移覆盖新函数。
