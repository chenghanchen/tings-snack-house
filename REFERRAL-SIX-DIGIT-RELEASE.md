# 唯一正式推荐码：TSHREF- 加 6 位字符

本方案取代 `REFERRAL-SHORT-CODES-RELEASE.md` 的长码兼容/别名方案。当前文件仅说明待发布升级，不代表已执行生产变更。

## 用户确认的规则

- 现有账户重新生成唯一的 `TSHREF-XXXXXX` 正式推荐码，后缀为 6 位大写字母/数字（排除 I、O、0、1）。新验证账户采用同样规则。
- 旧长码、旧 8 位短码都不能用于新订单。不保留兼容映射或别名。
- `customer_referrals.referral_code` 是唯一存储和使用入口；删除 `short_code` 列、别名索引/约束、触发器及函数，移除预览与下单中的别名查询。
- 历史订单快照、已有推荐事件和优惠券不修改。历史订单里留存的旧码只是审计文本，不会使旧码恢复有效；已成功订单的相同请求重放只返回原订单，不创建新订单。
- 后台按账户的推荐事件统计使用次数，不依赖旧码字符串，因此不会因换码丢失历史次数。

## 发布步骤与风险

1. 确认站点仍为用户所述测试阶段，保存数据库备份并记录升级前订单、奖励及优惠券指纹。
2. 按用户授权执行 `account-referral-six-digit-migration.sql`。这是一次会使旧码失效且删除别名数据的切换，UI 操作若要求操作时确认，应先说明影响并取得确认。
3. 脚本单事务锁定推荐/优惠券表，轮换既有账户码后恢复不可转让/更换保护；检查代码片段不匹配则回滚，不跳过校验。不用 CASCADE 删除未知依赖。
4. 该脚本可重复执行，不重复轮换已完成迁移的 6 位正式码。不要重跑旧别名、钱包或营销迁移覆盖当前函数。
5. 验证每个已验证账户有且只有一个新格式正式码；`short_code` 及别名函数已不存在。旧码预览无效，新码有效；不为验收提交真实订单。
6. 发布后台更新，检查账户页、复制内容、下单成功页与后台一致。前台原有钱包/结算接口无需变化。

回退页面不恢复旧码；不得为回退重建别名。需要再次更换码时另行确认，不重复执行旧迁移。
