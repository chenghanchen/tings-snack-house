# 推荐新客终身一次（待发布）

## 已确认的规则

- 游客可使用推荐优惠，不强制登录；已登录者以服务端验证的 Auth UUID 和已验证邮箱为准，忽略请求中的伪造身份。
- 推荐资格不按推荐码分开计算。账户 UUID、规范化邮箱、规范化手机号任一匹配历史完成记录，均不再符合新客资格。
- 进行中的推荐优惠订单暂占资格；未完成即取消可以重试。订单完成后永久计入，之后取消/退款不恢复。
- 原有已完成普通订单也意味着不是新客，继续保持原规则。普通游客下单、奖励所有权和优惠券金额不改变。
- 邮箱去首尾空白并转小写，不擅自合并加号别名或点号。手机号统一为美国 10 位数字，支持 +1、前导 1、空格、括号、点和横线；拒绝分机、字母或其他国家码。
- 多账户共用手机号会被视为同一新客信号；游客邮箱/手机号并未经过实名验证。若同时换账户、邮箱、手机号且无历史关联，不能保证识别为同一自然人。

## 实现与历史边界

`account-referral-lifetime-migration.sql` 新增私有完成身份记录（不是推荐码别名），只存订单 ID、账户 UUID、邮箱摘要及规范化手机号。完成记录永久保留，不随订单取消、编辑或账户删除级联消失。推荐事件保留原貌。

迁移从现有已完成订单及已发奖/已撤回奖励事件回填，保留事件中的原始身份线索；不会把未完成取消算作完成。若迁移前某普通订单的完成状态已被覆盖且没有事件/审计证据，则无法推断过去是否完成；不可捏造这类历史。

预览和正式下单调用同一个资格检查；下单先规范化，再按账户/邮箱/手机号加事务锁，并保留幂等与原有唯一索引，防止跨推荐码重复占用。完成后发奖逻辑保持一次。

## 发布顺序

1. 先完成 `REFERRAL-SIX-DIGIT-RELEASE.md` 的正式推荐码切换（旧码失效、无别名），避免重跑旧迁移覆盖新函数。
2. 保存数据库备份并记录订单/事件/优惠券指纹，然后执行 `account-referral-lifetime-migration.sql`。它单事务锁定相关表，支持重跑，不改历史订单/事件/优惠券。
3. 发布 `supabase/functions/submit-order`（包含 `customer-identity.mjs`）以同步入口手机号规范化和限流；数据库同样校验，不能仅改页面。
4. 发布页面和后台文案/资源版本。只做只读生产验收，不提交真实测试订单或发真实奖励。
5. 验证未完成取消可重试、完成后不可重复、不同码共用资格、游客转注册继承历史、私有表不能被客户端读取。自动化验收在隔离数据库与模拟浏览器完成。

回退前端不应撤销永久完成记录。禁止重跑旧钱包迁移恢复旧资格函数。
