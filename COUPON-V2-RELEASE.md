# 优惠券领取与配送费券

2026-09-15：生产数据库已升级并验证。升级前后现有券数量和规则指纹一致，未创建测试券、领取记录或订单。此文件随本次网站版本发布。

## 发布顺序

1. 先确认现有 customer-wallet-migration.sql 已部署。
2. 在现有 Supabase 项目执行 coupon-claims-v2-migration.sql。它包含事务和重复执行保护，订单函数片段不匹配时会整体停止；不要忽略错误继续发布。
3. 确认新 RPC claim_customer_coupon、preview_account_offer_v2 与新版 get_my_customer_wallet 可用后，再发布前台和后台资源。
4. 不在生产环境用真实用户领取测试券或提交测试订单。此次测试均使用离线数据库、模拟账户和模拟数据。

## 保留规则

- 已有券默认 requires_claim=false，不补建领取记录、不改原到期时间、不追加折扣封顶。
- 推荐奖励仍由完成订单的服务端流程自动发放，不能手动领取。
- 新普通券仅允许已验证邮箱的登录账户领取，每账户每券最多一次；按账户而非收货电话限制。
- 领取占用发放名额，即使过期也不释放为新的领取次数；同一账户重试返回原领取结果和原到期时间。
- 新百分比券必须填写正数最高减免金额；最高减免只限制商品优惠，不含税和配送费。
- 新券需填写最低消费及领取后有效天数。若有活动截止时间，实际到期取“领取后天数”和“活动截止”较早者，界面明确显示。
- 已有人领取的新券锁定金额、门槛、有效期、叠加条件等承诺；仍可停用或调整不低于已领取数量的发放总量。
- 免配送费券沿用当前配送说明、最低配送消费和地址流程，不新增或扩大配送区域；仅减免本单整笔配送费。自取、已免运费或未达门槛不可用。
- 每单仍只用一张券，继续遵守原活动叠加规则。商品税额不因免配送费券而减少。
- 输入百分比按既有后台 % OFF 含义：15 表示减免 15%，顾客卡片显示 8.5 折。

## 验证入口

- scripts/release-check.mjs：完整自动检查及真实 PostgreSQL 兼容数据库测试。
- scripts/check-customer-account.cjs：离线浏览器账户、领券、失败重试、选择、配送切换及响应式布局。
- scripts/check-referral-marketing.cjs：离线后台创建封顶折扣券、免配送费券及旧券编辑兼容。

卡片宽度依据弹窗可用空间响应：较宽卡片三列，较窄卡片把优惠信息上下排列，操作始终留在右侧。不可用历史券默认收起，可展开查看原因。
