# 顾客账户：开发完成后的启用清单

## 2026-09-13 内部测试发布更新

用户已批准将所有现有功能发布到原 Cloudflare Pages 地址。账户入口不再限制本地域名；登录页明确提示 Resend 测试收件限制，邮箱验证与服务端权限保持开启。地址 v2 已确认在线，下单服务为第 2 版，推荐钱包按 CUSTOMER-WALLET-SETUP.md 协调升级。本文件下方的本地开发限制与日期记录属于历史状态，不再代表本次入口策略。

网站链接仍是公开可访问的；“内部测试”说明不是访问控制。没有宣称完成多真实账户、真实新推荐订单履约和完整收件范围验收。

2026-09-11 状态：用户已执行账户迁移并提供成功截图，线上账户资料接口存在且拒绝匿名访问；已通过授权的 Supabase CLI 将 submit-order 部署为第 2 版，包含 customer-identity.mjs，保留 verify_jwt=true。用户确认已保存 Resend SMTP、收到 6 位验证码，并在本地完成登录、刷新保持会话、打开订单和资料页面。账户前端尚未发布。
部署前的 submit-order 第 1 版已备份到本机 QA 目录 submit-order-backup-20260911。部署后确认服务 ACTIVE，OPTIONS 返回 204，无效订单正文返回 400，无效令牌返回 401；这些检查未创建订单或扣库存，不等同于真实登录与下单验收。
不同顾客邮箱、非店主账户权限、完整真实下单及邮件过期／退出等验收仍须在正式启用前完成。当前测试邮箱也是店主邮箱，不能将这次单邮箱验收当作普通顾客权限隔离验收。不要只发布前端文件。

继续使用 Resend 测试发信，只能向其注册邮箱验证，尚无自有发信域名；见 [Resend 开发测试](RESEND-DEVELOPMENT-TEST.md)。线上继续游客下单，不公开账户入口。

## 第一版范围

- 邮箱 6 位验证码登录／首次登录注册；不维护顾客密码。
- 我的订单、折叠明细与费用分项、取消进度、分页及刷新；可按订单号／商品／规格搜索当前页，或筛选当前页状态，不冒充全量历史搜索。
- 再次购买先核对当前商品、精确规格及库存，预览后再次确认；价格／数量变化需重新确认。仅加入购物篮，保留原有商品，不创建订单，不沿用旧订单价格或优惠。
- 一个常用收货地址、姓名和电话；结算仅自动填写空白项。
- 收货资料包含未保存提醒、放弃修改、重新加载、关闭／刷新提醒；慢响应和保存失败不会覆盖正在输入的资料。
- 游客仍能下单。旧游客订单不按邮箱或电话自动归户。
- 顾客登录存储键为 `tings-customer-auth-v1`，与原有店主会话隔离。
- 按用户选择，本次不新增积分体系，保留现有优惠与奖励规则；优惠券／推荐奖励账户中心、多地址簿、Google／Apple 登录、账户注销自助流程不在本次范围。

## 暂不公开的开发模式

`customer-account.js` 仅在 localhost、127.0.0.1、[::1] 上创建顾客会话和账户入口。其他域名保持游客 checkoutHeaders，不初始化顾客会话。这里是产品开放控制，不是安全权限边界；真正的数据隔离仍由 Supabase 验证和数据库权限完成。
本次账户中心完善只改本地前端和测试，没有再次运行线上迁移、更新服务或发布网站。无需重新执行已部署的账户迁移。
正式开放前，在完成剩余验收后明确修改此入口开关。不要靠公开 URL 参数或浏览器存储临时绕过它。

## 完整启用顺序（已完成项目以上方记录为准，不要重复执行）

1. 备份数据库，在独立测试项目验证下面的迁移。确认现有订单保护、限制公开下单和取消申请 v2 迁移已执行。
2. 确认店主 `chenghanchen1@gmail.com` 在 `auth.users` 中已有已验证邮箱。执行 `customer-accounts-migration.sql`。它固定店主 UUID，不从顾客可编辑资料授予管理员权限。缺失店主或遇到未识别的 owner RPC 会使事务整体回滚，需要先人工检查，不能删除安全检查强行执行。
3. 检查实际项目所有 public 表、Storage 策略和 SECURITY DEFINER 函数；仓库没有完整的初始线上 schema。本地测试针对已有迁移建立的测试结构，不能代替这一步。重点排除“authenticated 即管理员”的策略及未在仓库中的订单查询／修改接口。匿名和普通顾客不得读取他人的订单、地址、电话、后台备注、优惠券或推荐奖励明细，不得修改商品、库存、设置或上传／删除媒体。
4. 部署更新的 `supabase/functions/submit-order`。保留原有服务密钥、订单限流盐和来源白名单，确认 `SUPABASE_ANON_KEY` 与前台使用的公共 anon key 一致。服务端通过 `auth.getUser(token)` 验证顾客，不接受请求体声明的 user_id；匿名、过期或伪造用户 token 不会冒充已登录顾客。所有真实顾客都必须拥有已验证邮箱。
5. 配置 Supabase Auth 邮箱登录、允许新用户注册和自定义 SMTP。可后续选 Resend，但不能把 SMTP 密码或 service-role key 放入前端或 Git。
6. 设置验证码邮件模板（Magic Link 模板改为包含 `{{ .Token }}`），确认项目实际 OTP 长度是 **6 位**。建议有效期 10 分钟、发送间隔至少 60 秒；启用服务端发送／验证限流，开放营销前评估 CAPTCHA。前端倒计时只是体验，不是防滥用控制。配置测试／正式 Site URL 与允许的跳转 URL；本实现输入验证码验证，不自动消费 URL 中的登录 token。
7. 使用两个真实测试邮箱，完成下面验收，再发布前端。若项目使用新的 JWT 签名方式，核对 Edge 网关对用户 token 的支持；不能仅为了消除 401 而绕过函数内的身份验证。

Supabase 默认邮件服务有收件人和发送限制，不能当作正式营业邮件服务。参考：
[邮箱验证码](https://supabase.com/docs/guides/auth/auth-email-passwordless)、
[自定义 SMTP](https://supabase.com/docs/guides/auth/auth-smtp)、
[行级访问控制](https://supabase.com/docs/guides/database/postgres/row-level-security)。

## 数据与安全设计

`customer_profiles`、`customer_addresses` 每位顾客一行，主键关联 `auth.users.id`。
资料保存和账户订单查询只通过当前已验证会话的 `auth.uid()` 定位，不接收目标顾客 ID。
订单关联在同一个数据库事务内完成，幂等键重试必须同时匹配原请求和原账户。
账户订单对游客查单／取消接口不可见；账户读取接口只返回顾客所需字段，不返回店主备注。
订单的用户外键没有 `ON DELETE SET NULL`：不能因为删除登录账户而让私有订单变成游客订单。
处理账户删除请求时，应另行设计订单保留／匿名化流程，不能直接将 `orders.user_id` 清空。

迁移会为已知店铺表追加限制性写权限，为敏感表追加仅店主可读限制；保留已有允许策略。
已知 `owner_*` 函数改用固定店主 UUID 检查，并撤销 PUBLIC／anon 的执行权。
2026-09-10 用户提供的线上预检确认：现有表均已启用 RLS，订单必需字段及幂等函数存在，三个 owner RPC 的旧校验均可识别；另发现公开的旧 `cancel_customer_order(text,text)`。迁移已将其和旧 `lookup_customer_order` 一并停止向游客／普通顾客开放，保留当前前台使用的查单与取消申请接口。此次核对只针对预检元数据，尚不代表线上迁移或真实邮件验收已完成。
之前的公共下单函数保留给 service_role；新函数 `submit_shop_order_account` 也仅该角色可调用。
不要重新运行旧迁移覆盖新的函数或权限。

## 验收与本地测试

### 收货资料 2.0（2026-09-12，本地开发）

先执行基础账户迁移，再执行 `customer-address-v2-migration.sql`。新脚本只升级默认收货资料与对应 RPC，不修改订单、店主策略、库存或提交服务；可重复执行。不要在其后重跑旧基础迁移覆盖新函数。

- 新增 Unit、City、State、ZIP。旧完整地址保留在地址字段，不自动猜测拆分。州缩写规范为大写，ZIP 支持五位或 ZIP+4；空白字段允许保存草稿，结算仍执行现有必填校验。格式校验不等同于真实地址核验。
- `save_my_customer_details_v2` 接受七个资料字段，不接受邮箱或用户 ID；身份只来自 `auth.uid()`。登录邮箱在前端只读显示。旧三字段保存接口保持兼容，并清空旧客户端无法表达的结构化后缀，避免拼接重复地址。
- 默认地址自动合并填入现有结算地址栏。只有账户页点击“保存资料”才保存默认值；结算的临时修改和旧订单不会回写默认资料。
- 新脚本未执行时，保存会明确提示需要数据库升级，不会静默丢弃新增字段或假报成功。此轮不自动发布前端，也不执行线上数据库升级。
- 登录过期时停止账户保存，显示重新登录入口；重新登录前放弃未保存修改会再次确认。网络失败不应显示成“暂无订单”。
- 再次购买仍只操作本地购物篮，核对当前在售商品、规格、价格与库存；正式提交继续调用原 `submit-order` 服务，不增加直接写入订单的通道。

安装开发依赖后运行：

```text
npm install
npm test
npm run release:check
npm run test:accounts:browser
```

浏览器测试默认使用本机 Edge；其他环境可设置 `TINGS_BROWSER_CHANNEL=chromium` 并安装对应 Playwright 浏览器。
若使用外部已安装依赖，可设置 `TINGS_PGLITE_MODULE`（模块文件 URL）和 `TINGS_PLAYWRIGHT_MODULE`（包目录）。
浏览器测试拦截全部网络、使用模拟数据，不发送真实邮件，不创建线上订单。
数据库测试在内存 PostgreSQL（PGlite）执行真实迁移，包含 RLS、越权、取消、幂等键身份匹配和重复迁移测试。

正式启用前另外实测：

- 邮件送达、错误／过期验证码、重发、刷新后保持登录、退出和多标签页退出同步。
- A 只能读取 A 的资料／订单，B 和游客不能读取或取消 A 的订单；相同电话号码也不能越权。
- 顾客不能进入店主管理功能，店主功能、库存、价格计算和订单通知仍正常。
- 游客与登录顾客均完成一次真实测试下单；重复提交不重复扣库存，账户切换不能重用原幂等键。
- 手机与电脑上的账户入口、键盘焦点、资料自动填充、订单列表及取消申请可用。

## 回退

如果暂时不开放账户，先隐藏前端账户入口并保留新的订单后端和权限保护；不要回退为公开可查询账户订单的旧 SQL。
已存在账户订单后，不应撤销用户外键或删除账户表来“恢复游客模式”。数据库回退需先备份并单独评估数据访问风险。
