# 邮箱账户推荐奖励系统

本版替换未部署的人工手机号认领草案。2026-09-13 用户批准所有功能按内部测试版上线；地址 v2 和账户下单服务已在线，奖励迁移已通过线上事务回滚演练，配套前端开放所有域名上的账户入口。最终发布结果以本次部署验收为准，不以本说明代替。

## 固定规则

- 邮箱 OTP 验证后的 auth.users.id 是唯一账户身份。每个已验证账户自动生成一个永久、不可转让的推荐码。
- 手机号仅作收货联系及防滥用信号，不生成推荐人、不查询奖励归属、不认领奖励。
- 游客可用他人推荐码，但需提供邮箱；游客不会生成推荐码、不会拥有奖励钱包。
- 新客首个有效订单满 $30 减 $5。推荐优惠与优惠券共用一个兑换入口，每单至多一张；既有活动叠加规则仍由服务器核算。
- 下单只建立 pending 推荐事件，不发券。订单进入“已完成 / 已取货 / 已配送”，且没有未处理取消申请后，才给推荐人发一张 $5 OFF、满 $30 使用、90 天有效、限一次的账户券。被推荐人不再同时获赠另一张券。
- 取消待完成订单不发奖，并释放该笔待处理推荐资格。重新下单仍需通过完整新客校验。
- 已发奖的订单后来取消：券停用，事件标为 revoked，不再次发券。已使用的券保留核销审计，不自动追扣、不修改另一笔订单金额。
- 已取消事件不随旧订单重新标记完成而复活；需重新下单验证。普通订单不会补发历史奖励。

## 身份与风控

提交订单的 user_id 只来自 submit-order Edge Function 验证后的 Auth 用户。登录账户使用 Auth 中的邮箱校验，不能伪造结账邮箱逃过自荐或首单限制。邮箱更新后，原事件的邮箱去重记录仍保留。

服务器结合账户已有完成订单、规范化邮箱的历史完成/新人优惠记录、推荐事件及已用券记录去重。联系方式与推荐人本人已知联系方式相同，或该联系方式已有完成订单/有效推荐事件，也会保守拒绝新客优惠，但绝不据此转移奖励。

这可能使共享电话号码的家庭成员无法分别领取首单优惠。未验证邮箱的游客仍可换邮箱和号码冒充新人，无法保证“一个自然人一次”；本版仅保证已知身份信号不重复。继续保留现有订单限流，正式经营中需结合履约核实；更强防滥用需要验证游客邮箱或额外服务，不应宣称已解决全部刷单。

## 数据关系

- customer_referrals.referrer_user_id → auth.users.id；每账户一个唯一推荐码。
- referral_events.referrer_user_id 必填；referred_user_id 可空；referred_order_id 唯一。
- referral_events 保存 pending / rewarded / cancelled / revoked / ineligible 状态及 reward_coupon_id。
- marketing_coupons.claimed_by_user_id → 奖励所属账户，source='referral'。recipient_phone 为空。
- coupon_redemptions.user_id 记录核销身份。换收货号码不能重用已核销券。
- 邮箱去重键目前是受权限保护的 MD5 索引值，并非匿名化或加密；普通顾客不能读取它。原始联系方式仍在订单内。

推荐人的账户页面仅返回自己的码、券和事件状态，不返回被推荐人的订单号、手机号、邮箱或姓名。后台通过店主权限查看事件对应订单。游客之后注册，只生成自己的码，不认领游客订单。

## 本地功能

账户中心：我的优惠券（普通券、新人券）、推荐奖励（推荐码/复制、可用/已用/过期券、推荐记录）。
结算页：账户券单选，亦可手动填兑换码；服务器重新校验资格、门槛、有效期、次数和活动冲突。
营销中心：账户推荐规则与事件状态、账户归属；不再提供旧版按手机号调整推荐的入口。普通券支持固定金额与百分比。

## 上线顺序与旧数据

1. 备份生产数据库，并确认 customer-accounts-migration.sql 和 customer-address-v2-migration.sql 已完成。
2. 在测试环境执行 customer-wallet-migration.sql。它在单个事务中升级，并检查旧订单函数的明确代码片段；未知版本会中止，不能跳过检查。
3. 若已经安装过人工手机号认领草案，该脚本会停止，需单独处理，不能自动清空已有归属。
4. 旧 customer_referrals 手机号行、旧营销券和订单原样保留；不推断这些记录属于哪个邮箱。旧手机号推荐码和未归属账户的个人奖励券不再可用，切换前应由店主另行决定历史权益如何处理。
5. 协调执行生产迁移并发布匹配前后台。旧前台推荐预览会在新数据库中被停用，不能长期混用两个版本。用户已批准内部测试开放账户入口；Resend 测试发信限制仍保留，正式推广前需要验证自有发信域名和多账户验收。
6. 不要在新迁移后重跑旧营销或账户函数脚本，否则会覆盖核销与发奖保护。回退只隐藏入口，不恢复旧的不安全提交函数或删除归属/事件数据。

Edge Function 的接口与限流保持不变，本次无需新增短信供应商。此文件及测试通过不代表线上数据库已经更新。
